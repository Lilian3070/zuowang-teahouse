# 不许再犯

给以后的自己看的清单。**每次动手写代码前先扫一遍这一页**，每条都是已经犯过、被主理人当场
指出来的问题——重复犯同一个错比犯新错更不能接受。

新踩的坑往下加，格式统一：**规则 → 当时怎么犯的 → 以后怎么做**。

---

## 1. 同一个界面里的操作，不许整块重刷／闪一下

**规则**：只要用户还停在同一个界面（翻页、切换、选中、筛选……），画面就不该出现"内容消失
→ 转圈/占位 → 重新长出来"这种过程。也不许点了按钮半天没反应。

**怎么犯的**（2026-09-04，首页预约周表）：点 ‹ › 翻周时，代码是
`wrap.innerHTML = '<div class="bwk-loading">查询空档中…</div>'` 先把整张表清空，再等网络请求
回来重建——于是每翻一次表格都"闪"一下、滚动位置也跟着归零。拖拽选时间时更夸张：每移动一格
就把整张表重画一遍。

**以后怎么做**：
- 数据没回来之前**保留原有结构**，最多把要变的那块做局部占位/淡入，绝不清空重建
- 能缓存的一律缓存（这次是按"周"缓存），**并且预取相邻的**（翻到某周就顺手预取上一周/下一周），
  让常见操作直接零等待
- 高频交互（拖拽、悬停、选中）只改必要的那几个 DOM 节点／class，不要重建整棵树
- 异步结果回来先确认"用户还停在原来那个位置"（见第 5 条），不然会闪回旧内容

⚠️ 这条在记忆里本来就有一条"流畅优先原则"，2026-09-04 还是又犯了一次，所以单独立成第 1 条。

---

## 2. 改完 `script.js` / `styles.css`，必须升 `index.html` 里的 `?v=` 版本号

**怎么犯的**（2026-09-04）：改完 JS 忘了升版本号，主理人手机上"雅室预约"按钮死活不出现——
浏览器跑的还是缓存里的旧 JS。当成代码 bug 排查了半天，其实是缓存。

**以后怎么做**：提交前 `grep "?v=" index.html` 对一眼，改了哪个文件就升哪个的号。

---

## 3. 做新功能前，先看这个网站已经有没有现成的一套，直接照搬参数

**规则**：不要凭印象"做一个像的"，要把已有实现的**数值**抄过来。

**怎么犯的**（2026-09-04）：客人端预约选时段，本质就是"简化版的后台周表"。我没去看
`admin.html` 的 `renderCalWeek`，自己设计了下拉 → 合并区间 chip → 半小时 chip 列表，返工三四轮
才回到"就是那张表"。就算结构对了，参数还是全不一样（时间标密度、格子高度、边框色、隔天底色
……8 处），看着就不像一个系统。

**以后怎么做**：先打开对应的已有实现，把颜色/尺寸/字号/间距**逐个抄**；差异只允许出现在
"这个角色不该看到什么"上，不在视觉规范上。

**又犯了一次**（2026-09-05，预约核对横幅）：给新按钮挂了 `class="btn-add-inline"` 就以为拿到
样式了，结果渲染出来是**浏览器原生按钮**——因为 `.btn-add-inline` 的底色/圆角只写在
`.add-inline-form .btn-add-inline` 和 `.cal-toolbar-right .btn-add-inline` 这两个**带父级**的
选择器里，新按钮不在那两个容器下面，一条规则都匹配不上。主理人一眼看出"按钮样式跟网页设计
不匹配"。讽刺的是这个坑文件里本来就有注释写着（那行注释就是上次补 `.cal-toolbar-right`
样式时自己写的），我没看。

**所以这条要再加一句**：复用一个类名之前，先 `grep` 一下它到底定义在哪——**很多类的样式是挂在
"父级 + 类名"上的，光挂类名拿不到**。抄参数时连**选择器的作用范围**一起看，别只看属性值；
拿不准就把那几行属性直接抄一份到自己的选择器下（这次就是这么修的）。

---

## 4. 数据库写操作后面，必须先检查 `error` 才能提示成功

**怎么犯的**（2026-09-02，全文件性问题）：`insert/update/delete` 之后直接弹"已保存"、关弹窗，
数据库真的报错了界面上完全看不出来。

**以后怎么做**：`const { error } = await db.from(...)...;` 后面第一件事就是
`if (error) { 提示失败; return; }`，再谈成功提示/关弹窗/跳转。

---

## 5. 异步结果回来，先确认用户没切走

**怎么犯的**（2026-09-02，后台切栏目）：上一个栏目的查询晚回来，照样往页面上写，把已经切过去的
新内容盖掉了。

**以后怎么做**：发请求前记下"当前是哪个栏目/哪一周/哪个 id"，结果回来先比对，不一致就直接丢弃。

---

## 6. 一条消息里提了几件事，就要几件都做

**怎么犯的**（2026-09-04）：主理人一次列了好几处要改，我只改了其中一处就回话了。

**以后怎么做**：动手前把要求拆成清单，改完逐条对照确认，回话时也按条说明。

---

## 7. 不用浏览器原生 `confirm` / `alert` / `prompt`

**规则**：任何需要用户确认或输入的地方，一律用站内风格的弹窗（`customConfirm` / `customPrompt`
或同风格 modal）。黑底系统框跟站点视觉完全不搭。

---

## 8. 日期/时间一律用站内自制选择器，不用原生 `<input type="date|time|datetime-local">`

原生控件英文显示、样式不统一、部分浏览器点两次关不掉。站内已有 `dtPicker*`（后台）和预约周表
（前台）两套可复用。
