# 坐忘茗舍网站 · 交接文档

最后更新：2026-09-05
备份检查点（可回滚）：`checkpoint-2026-09-04-b`（回滚命令见文末）

> ⚠️ **动手改代码之前，先读同目录下的 [`不许再犯.md`](不许再犯.md)** ——那是已经犯过、
> 被主理人当场指出的问题清单（画面闪跳、缓存版本号、照抄已有实现的参数……）。
> 重复犯同一个错比犯新错更不能接受。犯了新的重复性问题，也往那个文件里加。

## 开新对话先看这里（2026-09-05 收工状态）

**这份文档要整份读完，不要只读结尾几十行**——2026-09-04 就因为只读了后 150 行，漏掉第 27 行
写着的 `?v=` 缓存约定，白排查了半天。

**2026-09-05 做完、已经上线的**（首页"雅室预约"那张表，按主理人要求跟后台故意拉开差异——
客人不写备忘录，不需要照搬后台读行程的那套读法）：
1. **去掉左边整根时间轴列**，时间改成淡灰色印在每个能约的格子里；列宽 46px → 58px
   （砍掉 46px 的轴刚好补回来，一屏看到的天数没变少）
2. **不可约的格子不再只是灰斜纹**：连着的一段合并起来，整段盖一个"已约满"（被占用/主理人关了）
   或"已过"（时间已经过去），文字 sticky 跟着滚动走，客人一眼看得懂为什么点不了
3. **拉出选区后不再逐格印时间**：选中范围内的格子把时间藏掉，那一段只由选框上的
   "开始时间 / 共几小时 / 结束时间"三个标交代；顺手修了两个旧缺口——时长标门槛偏高
   （选 1 小时显示不出"1小时"）、只选半小时时两个时间标会被 24px 的框裁掉半行
4. 表格下面那行"已选：…"补上总时长，选框太矮写不下时由它兜底

**2026-09-04 做完的**：
1. 后台"关闭/开放时段"批量框选模式的全部视觉细调（红色细虚线框+斜纹、月视图虚线角标=部分关闭 /
   实心印章=整天关闭、编辑模式预选框的起止时间与时长标签）
2. 首页"已是知音"改版：卡片只留"雅室预约"按钮 → 点开站内弹窗 → 弹窗里是一张**照抄后台周视图**
   的表格，实时显示茶室真空档，可拖拽选时间段（详见下面那节）
3. 新建 `不许再犯.md`（重复错误清单），交接文档开头加了硬提醒

**SQL 状态：现网 SQL 全部已经跑过，不用再动**（09-05 这天没有新增任何 SQL）。最近几次分别是
09-04 的 `supabase/tea_busy_ranges.sql`（建 `get_tea_busy_ranges()` 函数，主理人当天已在
Supabase SQL Editor 执行，返回 Success）、09-03 的 `supabase/schedule_closed.sql`、
09-02 的 `supabase/schedule_resource.sql`。
`supabase/` 目录里这些文件留着只有两个用途：① 以后**换新的 Supabase 项目/从零重建数据库**时，
照文件名顺序重跑一遍；② 查某个字段/函数当初是怎么建的。日常开发不需要跑任何 SQL。

**当前缓存版本号**：`styles.css?v=35`、`script.js?v=33`（改这两个文件记得 +1，见第 27 行那条）。

**接下来最可能要做的**（按优先级，细节看文末"待做事项"）：
- 主理人还在逐处细看首页预约表格的体验，可能继续微调
- 真正的"提交=锁定时段"强占用（现在还是人工确认兜底）
- 实时天气（第 7 点，锦上添花，API 密钥不能明文放前端）
- 前台整体布局与视觉的持续打磨

## 基本信息

- 本地代码：`C:\Users\xianl\Desktop\Lilian\坐忘茗舍网站\`
- GitHub：https://github.com/Lilian3070/zuowang-teahouse
- **正式网址：https://zuowangmingshe.com**（Cloudflare CDN + GitHub Pages）
- 后台地址：https://zuowangmingshe.com/admin.html
- 部署方式：push 到 GitHub main 分支，约 1 分钟内自动生效
- Cloudflare Page Rule：`*zuowangmingshe.com/*.html` → Cache Level Bypass（HTML 不缓存）

## 技术架构

纯静态 JAMstack：`index.html`（前台）+ `script.js` + `styles.css` + `admin.html`（后台，内嵌样式和脚本）

数据库：Supabase，Project URL `https://wmatsdnpbpcltuoynyrh.supabase.co`，Publishable key `sb_publishable_gw7gfFtSHHJR6SbDUpCTwA_HNcktgEl`，区域 Southeast Asia (Singapore)。

**前端库不再依赖 jsdelivr CDN**（国内偶尔连不上/慢，一旦失败前台商品和后台会整体失效），全部自托管在 `assets/vendor/`：
- `supabase-js@2.112.4.min.js`（index.html + admin.html 共用）
- `fullcalendar@6.1.15.global.min.js`（admin.html 日程日历）
- `fullcalendar-locale-zh-cn@6.1.15.global.min.js`（原来引用的 `locales-all.global.min.js` 在该版本包里根本不存在、一直 404，日历中文语言包从未生效；已换成真实存在的路径）
- **以后升级这几个库需要手动下载替换文件**，不会像 CDN 那样自动跟最新版

**CSS/JS 缓存刷新**：每次改 `script.js` 或 `styles.css` 后，把 `index.html` 里对应的 `?v=` 版本号 +1。当前：`styles.css?v=35`、`script.js?v=33`（这个数字容易过期，以文件开头"开新对话先看这里"那节为准）。
⚠️ 2026-09-04 踩过一次：改完 `script.js` 忘了升版本号，主理人手机上"雅室预约"按钮一直不出现（浏览器跑的还是缓存里的旧 JS，压根不知道有这个按钮要显示），排查半天才发现是缓存问题不是代码问题。**改完 JS/CSS 提交前先 `grep "?v=" index.html` 对一眼。**

## 数据库表结构

### categories
`id, type(tea/teaware/guqin), key, name, description, cover_url, sort_order, is_visible`

分类 key 对照：
- tea：zhengshantang(正山堂·骏眉中国) / hexingyan(和星岩) / pinguvillage(品古村)
- teaware：zhuchaqi(泡茶主器) / pinmingbei(品茗杯盏) / chaxiyashe(茶席雅设) / mingyao(名窑匠作)
- guqin：chuxian(初弦) / miaoyin(妙音) / cangfeng(藏锋) / xiexing(携行)

### products
`id, category_key(字符串，对应上表 key，不是 UUID), name, description, price, image_url, detail_images(text[]，商品详情图片数组), is_visible, sort_order, created_at`

### posters
`id, section(qindao/yaji), title(显示文字，选填), image_url, is_visible, sort_order, created_at`

## 已完成功能

### 前台（index.html + script.js + styles.css）
- 精选茗茶 / 茶器美学 / 斫琴甄选三个板块从 Supabase 动态读取分类和商品，隐藏分类不显示
- 商品弹窗预缓存全部商品，点击秒开；商品"查看详情"弹出图片画廊（主图+详情图，缩略图/左右箭头/键盘切换）
- 移动端（≤860px）分类/海报用横向轮播展示，首次加载即生效（不用等用户手动触发一次 resize）；轮播启动后 0.9 秒就开始第一次切换，不会让人误以为只有一个分类
- 海报板块（琴道传习/坐忘雅集）横排展示，卡片数量少时居中，多到装不下时轮播
- 顶部导航栏**全部 8 个菜单项都有子菜单**：
  - 精选茗茶/茶器美学/斫琴甄选：子菜单内容跟分类数据同源，后台改分类会自动同步
  - 琴道传习/坐忘雅集：子菜单用每张海报的"显示文字"，没填文字的海报不出现
  - 茶席预约/坐忘故事/寻访茗舍：固定文字子菜单，跳转到板块内对应的小节（用 `scroll-margin-top` 避免被固定导航栏挡住）
  - 桌面端鼠标悬停展开下拉；移动端点主菜单名字本身展开/收起（不跳转），同一时间只展开一个，其他主菜单项自动顺延；子菜单里的具体项目才会真正跳转/弹出详情框
  - 点品牌/分类子项：跳转 + 直接弹出该项详情框；点海报/预约/故事/联系方式子项：直接跳转到对应位置（没有单独详情弹窗）
- 数据请求失败自动重试（最多 2 次，间隔 0.8 秒），避免手机网络抖动导致某个板块内容永久空白
- 首页大图（logo/店铺印章/联系方式图标）按实际显示尺寸压缩，省了约 2.1MB 首屏体积
- 商品详情弹窗（点分类卡片弹出的那个）在"一排天生只能放下 1 个商品"的窄屏下（用 `modalProducts.clientWidth` 按 `auto-fill, minmax(200px,...)` 的公式反算，不是写死的媒体查询断点），标题下方会出现一个每排显示几个的切换控件：三个空心方框图标（1 个方框 / 2x2 四宫格 / 3x3 九宫格，不写文字，靠图标本身表达，市面上常见的宫格视图切换样式），选中态高亮；方便小屏幕用户一次多看几件商品再点进详情。电脑/平板宽度只要天生能放下 2 个以上就不会出现这个控件，也不受影响。选择会记到 `localStorage`（key: `modalGridCols`），下次打开任何分类都记得上次选的排列方式

### 后台（admin.html）
- 登录/退出、修改密码
- 商品 CRUD，图片 canvas 压缩（最大 1200px，quality 0.82）上传 Supabase Storage
- 商品主图带裁剪框（4:3，跟前台卡片比例一致）：可拖动改变显示区域、滚轮/滑块缩放，不强制居中，保存时才把当前裁剪结果烘焙成一张图上传；只有真正动过图片（选新图/拖动/缩放）才会重新生成上传，没碰过就沿用原图，不会白白产生新文件。编辑老商品时，就算是历史上传的旧图（不是当初裁剪出来的）也能直接在这个框里重新调整
- 后台商品列表缩略图改成 4:3（跟前台卡片、裁剪框比例一致，之前是 1:1 方形裁切，跟前台实际显示不一样，看不出图片有没有裁歪/裁漏）
- 保存商品（新增/编辑）后不再整体刷新列表：只原地替换/插入被改动的那一行，其余行的 DOM 节点原样不动，不会有整页闪一下的感觉；如果编辑时把分类改到当前筛选范围之外，那一行会从视图里移除（数据本身还在缓存里，切换筛选能看到）
- 保存商品彻底改成"界面立刻乐观更新 + 图片上传/数据库写入放到后台"：点保存后弹窗立刻关闭、列表立刻显示新内容（图片用本地裁剪好的预览，不用等 Supabase 上传），网络操作在后台默默完成，成功了才把预览图换成真实链接、清理旧文件；失败了会弹提示并把界面改回原样（新增的会把那一行整个撤掉）。彻底解决了"点保存要等一两秒才关窗口"的卡顿感
- 上面这个后台流程里，正式图片链接生成后不会立刻塞进 `<img src>`，而是先用一个隐藏的 `Image()` 把它预加载/解码完，再去替换列表里的那一行——这样切换到真实链接的瞬间浏览器已经有缓存了，不会看到"缩略图慢慢加载出来"的效果
- 商品详情图片：可加多张、随时增删，保存时才真正上传
- 图片上传区支持真正的文件夹拖拽（之前文字写了但没接线）
- 图片替换/删除时清理 Storage 孤儿文件（商品主图、详情图、分类封面图、海报）
- 分类管理：CRUD、排序、显示隐藏，切换 tab 时正确收起/刷新（之前有 CSS 选择器碰撞导致的旧数据残留 bug，已修）
- 商品编辑弹窗去掉了多余的"显示此商品"勾选框（显示/隐藏已由列表按钮控制，编辑保存不再覆盖该状态）
- 会员管理：琴人档案（课时套餐）、茗客档案（消费记录）
- 行程安排：FullCalendar 日历（中文语言包已修好）
- 移动端小屏幕下菜单栏改为横向换行排列，不再竖排占位；顶栏用回真实类名，紧凑显示

## 踩过的坑（给以后的自己提个醒）

**`.nav ul` 选择器碰撞**：写导航子菜单时，`.submenu` 本身也是个 `<ul>`，会被 `.nav ul` 这条通用规则的**每一条属性**顶替（display、position、transform、max-height 都中过招）。教训：给一个新组件加样式时，如果它的标签名/结构会被某条更早、更宽泛的选择器意外匹配到，不要一条属性一条属性地打补丁，而是一次性用更高优先级的选择器（比如 `.nav .submenu`）把这个组件需要的所有属性都接管过来。

**删除按钮曾经"先删存储、不管数据库是否删成功"**：`deleteProduct`/`deletePoster` 原来没检查 `db.from(...).delete()` 的 `error`，一旦数据库删除失败（网络抖动、权限问题），还是会往下执行把 Supabase Storage 里的图片删掉——结果是数据库里那条记录还在，但图片没了，前台会看到裂图。已修复：两处都改成先判断 `error`，删除失败就直接提示并 return，不再动 Storage。`deleteCat` 从一开始就有这个判断，是对的写法，以后任何新增的"删记录+删图片"逻辑都要照这个顺序写。

**2026-09-02 发现这不是孤立问题，是全文件的系统性漏洞**：主理人反馈新加的琴课行程点了"保存"、
弹出"已保存"提示，但列表里根本没出现——查出来是 `schedule_events` 表还没跑 `resource` 字段的
SQL 迁移（数据库层面真的插入失败），但 `saveEvent()` 压根没检查 `db.from(...).insert()` 的
`error`，报错也照样弹"已保存"、关弹窗，界面上完全看不出来失败了。顺手排查了全文件所有
insert/update/delete 调用，把同样"不检查 error 就提示成功"的地方（行程增删改、预约申请、
会员增删改、课时套餐、消费记录等十几处）都补上了 `if (error) { showToast(...); return; }`。
**教训**：任何 `db.from(...).insert/update/delete(...)` 后面如果紧跟着 `showToast('已保存')`
之类的成功提示、或者关闭弹窗/跳转，必须先解构 `{ error }` 并判断，绝不能假设网络请求一定成功——
这条比"删记录+删图片顺序"那条更根本，以后新写任何一处数据库写操作，第一反应就是要检查 error。

**商品图片裁剪框在 canvas 上重新画已上传到 Supabase 的图不会 "tainted canvas"**：本来担心跨域图片（Storage 公开 URL 跟站点不同源）画到 canvas 上再 `toBlob` 会被浏览器当成"污染画布"直接报错，实测只要给 `<img>` 元素设置 `crossOrigin = "anonymous"`（在赋值 `src` 之前），Supabase Storage 的公开 URL 默认就带了正确的 CORS 头，能正常读出像素、正常导出。这也是"编辑老商品时能对着历史图片重新裁剪"这个功能能做的前提——以后如果哪天要对着任何 Supabase Storage 的图片做 canvas 处理（滤镜、水印之类），记得先加这行，別的什么都不用配置。

### 会员/管理员账号体系（2026-08-30 新增，当时新增 SQL（现网已跑），见本节末）

- 数据库新增 `profiles`（角色：admin/member，member 关联到 members 表）、`invite_codes`（一次性会员注册密钥）两张表，
  以及 `is_admin()` / `redeem_invite_code()` 两个函数，SQL 见 [supabase/member_system.sql](supabase/member_system.sql)
- **部署后必须去 Supabase 后台 SQL Editor 手动执行这个文件一次**（把文件末尾的邮箱占位符换成实际管理员邮箱），
  在执行之前 admin.html 的登录会因为查不到 `profiles` 表而把任何人都当成"非管理员"登出——这是预期行为，跑完 SQL 就正常了
- 首页导航栏最右侧新增"登录"入口（未登录显示"登录"，登录后按角色显示"进入后台"或"会员中心"）；点开是登录/会员注册两个 tab
- 会员注册走密钥制：后台「琴人档案/茗客档案」→ 点进某个会员详情 → "生成一次性登录密钥"，密钥给到本人，在首页"会员注册"里填密钥+邮箱+密码换取会员身份，一码只能用一次
- admin.html 顶部新增"返回主页"按钮；登录时会校验 `profiles.role`，不是 admin 会被强制登出
- 新增 [member.html](member.html) 会员中心：登录后能看自己的课时套餐余额、消费记录（目前只读，还没有提交预约申请的功能）
- 登录/注册**支持邮箱或手机号二选一**，前端按有没有 "@" 自动判断；手机号在 Supabase 内部会包装成
  `<手机号>@member.zuowangmingshe.local` 这样一个假邮箱存进去，纯粹是技术兼容手段，客人完全无感，
  自始至终看到的都是"手机号 + 密码"。⚠️ 因为这个假邮箱收不到确认邮件，**Supabase 后台
  Authentication → Providers → Email → "Confirm email" 必须关闭**，否则手机号注册的账号会卡在
  "未激活"状态永远进不去（这个开关关掉是合理的——反正注册本来就靠密钥线下核实身份，邮箱确认是多余的一层）
- **忘记密码**：不走邮箱/短信（手机号账号的假邮箱收不到信），而是复用跟注册密钥一样的模式——后台
  会员详情新增"生成密码重置密钥"，客人拿密钥去首页"忘记密码"直接设新密码。原理是
  `reset_password_with_code()` 函数直接改 `auth.users.encrypted_password`（用 pgcrypto 的
  `crypt()`/`gen_salt('bf')`，跟 Supabase/GoTrue 用的是同一套 bcrypt 加密方式），不需要旧密码，
  也不需要主理人能看到密码本身（密码全程只有加密后的哈希值落库，包括管理员在内谁都读不出明文，
  这是正常且安全的设计，不是功能缺失）
- 后台会员详情"会员账号"区块现在会显示这个会员登录用的是哪个邮箱/手机号（`profiles.login_account`
  字段，注册时客人填的原文），方便客人忘记自己当初注册用的是哪个时，主理人能在后台查到告诉她
- **登录状态管理**（首页/admin.html/member.html 三处用同一套逻辑，改的话三处都要改）：登录框新增
  "记住密码"勾选框，靠一个自定义的 Supabase `auth.storage` 适配器实现——勾选了登录状态存
  `localStorage`（关浏览器也保持登录），不勾选存 `sessionStorage`（关标签页/浏览器就清空，不会
  留在店里公用电脑上给下一个人捡漏）。登录成功后会调用 `signOut({scope:'others'})` 踢掉这个账号
  在其他设备上的登录状态（同一账号同时只保留一处登录），admin.html/member.html 各自每 3 分钟轮询
  一次自己是否被踢；退出登录按钮用 `scope:'local'`，只结束本机、不影响其他设备。
  ⚠️ 浏览器自己的"要不要保存密码"弹窗（Chrome 原生功能）没法从网页代码层面完全禁止，只能把密码框
  标成 `autocomplete="off"` 降低邀请概率，公用设备最终还是要靠操作的人自己在浏览器弹窗里点"不保存"
- **admin.html 不再有自己的登录表单**：唯一的登录入口是首页。未登录状态访问 admin.html 只会看到一个
  "请先在首页登录管理员账号" + 跳首页的链接，跟 member.html 的处理方式一致
- **首页登录成功后留在首页，不自动跳转**：进不进后台/会员中心由用户自己点导航栏那个按钮决定（不勉强
  跳转，避免使用者还没准备好就被带走）。"踢掉这个账号在其他设备上的登录状态"
  （`signOut({scope:'others'})`）就在登录成功的当下直接执行——踩过的坑：如果紧跟着这行代码
  马上用 `location.href` 跳转页面，会把刚建好的本机登录状态一起冲掉，导致跳转过去的页面还要再登一次；
  现在登录后不跳转了，这个坑不存在了，写法上只要记得"别把 signOut 和页面跳转紧挨着写"就行
### 会员档案与账号解耦（2026-08-30 新增，当时新增 SQL（现网已跑）：`supabase/member_profile.sql`）

- `members` 表新增 `nickname`（备注昵称，主理人填，列表显示优先用这个）、`email`/`address`/`age`
  （客人自己在会员中心「个人信息」里维护的联系方式，跟登录账号是两回事），`name` 允许为空
- 后台新增「全部客户资料」列表（不依赖课时套餐/消费记录），标出每条记录是"已注册"还是"代建档·未注册"
- 后台新增"生成新客户注册密钥"（不挂靠任何已有档案）：客人拿密钥注册时，`redeem_invite_code()`
  会自动新建一条空白 members 记录绑定给她；原来"生成注册密钥（绑定这条档案）"还在，给已经有档案的
  老客人用，两条路径并存
- member.html 新增「个人信息」表单，姓名/电话必填、其余选填，客人自己维护，主理人在后台只能只读查看
- RLS 补了"会员更新自己的档案"这条 update 策略，没有这条会员中心的个人信息保存不了

### 茶席预约接入真实数据（2026-08-31 新增，当时新增 SQL（现网已跑）：`supabase/booking_requests.sql`）

- 新增 `booking_requests` 表：会员提交预约申请（期望到访时间、备注、状态：待确认/已确认/已回绝、主理人回复）。
  **只是"通知主理人人工判断"，不做真实的时段占用/冲突检测**——那一套需要"主理人忙碌时段屏蔽"+"茶室/琴课资源
  各自独立开放"，是个更大的可预约日历系统，设计还没想清楚，故意跟这次的预约申请解耦开，以后需要时再打通
- 首页"茶席预约 → 知音通道"原来是前端写死手机号白名单的原型，已替换：改成按登录状态判断，
  已登录会员直接看到预约表单，未登录提示先登录（"初次到访"那条不受影响，仍然是引导加微信联系）
- 知音中心新增"我的预约申请"，会员能看到自己提交的记录和状态
- 后台新增"茶席预约"tab，能看到所有申请、点确认/回绝，确认时可以顺手留句回复给客人（比如具体时间）

### 会员注册全流程真实测试 + 一批体验/数据修复（2026-09-01，SQL 都已跑过，无需再跑）

这天主要是拿真实注册流程跑通了一遍会员系统，过程中挖出并修了好几个 bug：

- 会员中心（member.html）从上下堆叠的长页面改成左侧菜单+右侧详情的布局：**个人资料 / 琴人中心（课时，约课功能还没做）/
  包厢预约（即茶席预约）/ 消费记录**，移动端菜单变成顶部横向胶囊按钮
- **已注册会员现在不能被后台删除**（这是这天理清楚的核心结论）：课时/消费/预约/个人信息只有一份数据，不存在"后台自己的备份"，
  删会员就是真删，连会员自己账号里也会清空。所以改成只有"代建档、还没注册"的空壳档案能删，已注册的删除按钮直接不显示，
  `deleteMember()` 里也加了服务端二次校验兜底。因此配套做的"误删找回登录账号"功能（`admin_rebind_login`）已经用不上，
  相关按钮/弹窗/JS 都删掉了——⚠️ 但 Supabase 数据库里那个函数本身还留着，是个无害的死函数，不影响任何功能，不用管
- 修了 `invite_codes.used_by` 外键没设 `on delete set null` 的坑：只要客人注册过、密钥用过，Supabase 后台
  Authentication → Users 里想删这个测试/客户账号会直接报"Database error deleting user"，删不掉。已修（现网数据库已跑过
  `supabase/fix_invite_used_by_fk.sql`），以后新建库照 `member_system.sql` 建表就不会再踩这个坑
- 修了 `deleteCode()`（会员详情页密钥旁边那个"删除"按钮）一直用不存在的 `id` 字段查询，点了没反应的 bug——
  `invite_codes`/`password_reset_codes` 两张表主键其实是 `code`（text），不是 `id`
- 密钥体验修了几处：用过的密钥自动从会员详情页消失（不再显示"已使用"占位）；生成新密钥前自动作废同一会员名下的旧未用密钥，
  任何时候最多一张有效；密钥展示弹窗（`codeDisplayModal`）之前 z-index 跟会员详情弹窗一样是 1000，在详情页里点生成密钥时
  其实已经弹出来了、只是被挡在下面看不见，关掉详情弹窗才露出来——改成 1001 保证一定浮在最上面；生成/删除密钥都改成乐观更新，
  不用等网络请求跑完才更新界面，去掉了肉眼可见的零点几秒卡顿
- 会员注册、忘记密码两个表单都加了"确认密码"字段，两次不一致直接拦下来，防止手滑打错以后登不进去
- 后台**彻底不用浏览器原生 `confirm()`/`prompt()`/`alert()` 了**，全部换成站内风格的弹窗（黑底系统框跟站点视觉不搭，
  主理人明确要求过）：新增了通用的 `customConfirm()`、`customPrompt()`，8 处删除确认、"新建客户档案"表单、
  茶席预约"确认并回复"都换掉了。**以后任何新功能只要需要用户输入/确认，都要用这几个工具或新建同风格 modal，
  不能再用原生对话框**——这是主理人明确提过的偏好，不是随口一说
- 桌面新增了两个指向正式线上网址的快捷方式（`坐忘茗舍网站.url` / `坐忘茗舍后台.url`），删掉了之前指向本地文件的旧快捷方式

### 后台体验修补 + 行程日历第一步自制（2026-09-02，当时新增 SQL（现网已跑）：`supabase/class_package_session_log.sql`）

- "全部客户资料"注册状态从文字"代建档·未注册"改成颜色区分（绿=已注册，红=未注册）
- "琴人档案"列表把"1 个套餐"改成显示还没上完的套餐真实名字，多个用顿号连接，全部用完显示"套餐已用完"
- 会员列表联系方式挪到名字后面括号里（非粗体，跟名字区分），第二行腾出来专门显示套餐/注册状态
- 扣课时改成先弹窗选上课日期再确认（防误点误扣），日期+操作时间存进 `class_packages.session_log`
  （新增字段，SQL 见上）供主理人自己在后台追踪，`member.html` 不读取展示给客人看；日期选择器是站内
  自制的中文日历弹窗，不用浏览器原生 `<input type="date">`（原生控件英文显示、部分浏览器点两次也关不掉）
- 会员详情页"添加套餐/添加消费记录"改成点了才弹窗填，不再一直平铺占地方
- **行程日历改成自己写的年/月/周三视图，不再套用 FullCalendar 库**（`assets/vendor` 里的 fullcalendar
  文件还留着没删，`<script>` 引用已经去掉了）：
  - 年视图：12 个月缩略图，有行程的日子标小圆点，点某天跳到月视图
  - 月视图：一格一天，超过 3 条行程折叠"还有 N 条"
  - 周视图：默认只画营业主时段 8:30-23:30，超出范围的行程自动往外扩（不会被截断看不见），点空白格
    默认建 1 小时行程，当备忘录快速加
  - 颜色：关联会员的行程用金色，个人行程/备忘录用青灰色，先跟主页色系一致，以后按需求加花纹
  - 这是"可预约日历系统"这个大需求的第一步（核心日历），后续要做的都记在下面的待做事项里
- 行程日历周视图改成半小时刻度（整点线实、半点线虚且淡），时间标签改成简洁的 `H:MM`；点空白格新建行程
  时起始时间吸附到最近半小时；时间轴上下各加"显示更早/更晚时段"手动展开按钮（可"收起"），跟原有的
  "行程超范围自动扩展"互不影响
- "添加/编辑行程"弹窗的开始/结束时间从原生 `<input type="datetime-local">` 换成站内自制的日期+时间选择器
  （日历选日期 + 半小时格子选时间，两样都选完自动收起），原生控件时间轮盘点开不好收、样式也不统一。
  做成了通用组件（`dtPickerInit`/`dtPickerGetValue` 等，按 `fieldId` 驱动），**今后任何地方要选
  "日期+时间"都复用这一套，不再用原生 datetime-local**——`newPurDate`/`recordPurDate`（消费记录日期）
  目前还是原生 `type="date"`，是历史遗留，之后顺手处理时也要换成同一套风格
- **行程加了"占用资源"字段（琴/茶/两者都占），周视图按左右分栏展示**，当时新增 SQL（现网已跑）：
  `supabase/schedule_resource.sql`（`schedule_events` 新增 `resource` 字段，老数据自动补 `both`）：
  - "添加/编辑行程"弹窗新增"占用资源"三选一：琴（红色伏羲式古琴简笔画图标）／茶（青花瓷盖碗简笔画图标，
    盖子微开）／两者都占，是必选项，不选不让保存
  - 周视图每天的格子按左右分栏：左边画琴，右边画茶，"两者都占"的行程通栏显示；每天表头有琴/茶两个小图标
    （不写字），中间有条淡淡的竖虚线分隔；点左半栏空白处新建行程默认预选"琴"、点右半栏默认预选"茶"，
    省一步手动选
  - 2026-09-02 二次修改：图标从伏羲式古琴/盖碗简笔画换成更简洁的设计——圆形线条框（`.res-badge`）
    配汉字"琴"/"茶"/"和"，`RES_BADGE_GUQIN`/`RES_BADGE_TEA`/`RES_BADGE_BOTH`（JS 常量），"添加行程"
    弹窗里的资源选择按钮是复制的一份静态 HTML（弹窗标签是页面加载时写好的静态 markup，没法直接引用
    JS 常量），以后要改图标样式两处都要改。配色跟事件标记打通：月/周视图的行程条不再按"是否关联会员"
    分金/青灰，改成按占用资源分——琴=红（`--red`）、茶=青花绿（`--sage`）、两者都占=金配"和"字前缀
  - 2026-09-02 三次修改：小篆字体没找到能自托管的开源资源，改成主理人直接手写琴/茶/和三个小篆字拍照，
    抠图（按笔画深浅把白底转成透明度）+ 分别上色，存成 `assets/icon-guqin.png` / `icon-tea.png` /
    `icon-he.png` 三张透明底 PNG，`RES_BADGE_*` 常量里从文字 `<span>` 换成 `<img>` 引用这几个文件。
    抠图脚本是临时用的，没有留在仓库里，以后要改字形得重新手写拍照走一遍同样流程。图标同时按主理人
    要求整体放大：日历表头 16px→22px，"添加行程"资源选择按钮 20px→30px
  - 这是"可预约日历系统"待做事项第 3 点的第一步：资源分栏的可视化和数据字段已经有了，客人预约页面
    共用视觉但按资源判断"能不能约"（第 3 点里"茶室以后是否只做自主泡茶"那个假设）还没做，见下面待做事项
- **修复了全文件性的漏洞：多处 insert/update/delete 不检查 `error` 就提示"已保存"**——起因是加"占用
  资源"那次，`schedule_events` 表还没跑 SQL 迁移，插入报错但照样弹"已保存"，用户完全看不出保存失败。
  排查后把行程增删改、预约申请、会员增删改、课时套餐、消费记录等十几处都补上了 `if (error) { ... return; }`。
  详见"踩过的坑"那节，**以后任何数据库写操作后面接成功提示前，第一反应就是要检查 error**
- **"添加行程"的开始/结束时间拆成日期+时间两个独立下拉**（原来挤在一个面板里），时间列表默认只显示
  8:30-23:30，上下"更早/更晚时间"按钮展开，逻辑跟周视图周历一致；`dtPickerState` 每个字段现在是
  `{date, minutes, calMonth, timeExtraEarly, timeExtraLate}`
- **开始/结束时间会自动避让同一天、同一条资源线上已有的行程**：开始时间里被占用的时段直接置灰
  （删除线样式）；结束时间最多只能选到"开始时间之后第一条冲突行程的起点"，超过的不显示，也不能选在
  开始时间之前/同一时刻。换日期/开始时间/占用资源都会重新算一遍（`updateEventTimeConstraints()`），
  面板开着时当场重画。编辑已有行程时冲突判断会排除它自己。⚠️ 这套判断是纯前端算的（依据周视图已加载
  的行程缓存 `tabProductsCache['schedule']`），不是数据库层面的强约束，多人同时操作仍可能冲突，暂时够用
- **日历加了农历日期、传统节日、二十四节气**（"可预约日历系统"待做事项第 6 点）：引入开源库
  `lunar-javascript`（6tail 的项目，MIT 协议），自托管在 `assets/vendor/lunar.js`，`admin.html`
  里紧跟 supabase-js 那行加了 `<script src="assets/vendor/lunar.js"></script>`。新增
  `getLunarLabel(date)`：优先显示阳历节日（元旦/劳动节等）＞农历节日（春节/中秋等）＞节气＞农历日期
  （初一显示"X月"），节日/节气用金色加粗（`.special`），普通农历日期用淡灰色小字。月视图在日期数字
  下面加一行，周视图表头日期后面加一小段。
  ⚠️ **这份 lunar.js 是精简版**：只保留了农历换算 + 节日 + 节气这几块核心功能能正常工作（已用春节/
  中秋/夏至等已知日期验证过换算结果都对）。原库还带的八字排盘、择日宜忌（黄历宜忌）、太乙九星、
  佛历、道历、法定节假日调休这些跟这次需求无关的模块，为了控制体积都换成了最小占位实现（调用不报错，
  但不提供真实功能）——以后如果要做"黄历宜忌"之类更细的功能，得去 6tail/lunar-javascript 仓库重新
  拿完整代码替换对应模块

### 手动关闭时段：从"弹窗开关"返工成"批量框选模式"（2026-09-03，当时新增 SQL（现网已跑）：`supabase/schedule_closed.sql`）

`schedule_events` 新增 `is_closed` 字段。**第一版**做成"添加/编辑行程"弹窗里的一个开关，主理人反馈
这是错的设计思路——"关闭时段"是批量的（外出一周、连续几天不开门），不该一天一条手动加。**当天返工成
第二版**：月/周视图工具栏一个独立的"关闭/开放时段"按钮，点了整个日历进入**批量框选编辑模式**：

- 月视图点单天切换关/开，**拖拽选一段日期范围**统一刷成跟起点相反的状态
- 周视图复用原有"拖拽预选时间段"机制（半小时吸附、左右分琴茶、跨中线变"两者都占"）
- 编辑模式整块日历套 `filter: grayscale()` 压暗，加提示横幅，一眼看出不是平时那个模式
- 选择过程只在内存两个 Map 里记，点"保存关闭设置"才一次性提交：按"天"为单位重算——现有关闭记录
  铺底 → 月视图整天 pending 覆盖 → 周视图半小时格 pending 精调覆盖 → 转回连续区间，整批替换那一天的
  记录。"整天关闭后又用周视图精调重开几小时"这种混着改的情况天然被覆盖，不用写特判
- **教训**：批量性的操作不该套用"单条记录表单"的交互模式，即使底层数据结构长得像"一条记录"。以后遇到
  "批量标记一批东西"的需求，先想清楚是"编辑一条记录"还是"框选一批东西做批量动作"

### 关闭时段的视觉规范（2026-09-04 多轮细调后定稿）

- 周视图部分时段关闭：**细红色虚线框 + 细红色斜线底**（斜线要细、间距要大），不跟真行程共用卡片样式
  （那样像备忘录），平时不写时间文字，只在编辑模式里显示
- 编辑模式预选框：开始时间贴框顶、结束时间贴框底、中间空间够就插时长（"1.5小时"），**横排**白底小标签，
  跟拖拽新建行程那套 `.cal-week-selectbox-label` 统一；块太矮装不下两个标签就只显示开始时间
- 整天关闭：月视图红色实心印章、周视图表头"闭"字小圆章 **＋** 时间轴上也贯穿画红色虚线框
- 月视图部分时段关闭：表头右上角小圆圈**虚线**角标（"关"字）。**虚线=部分关闭，实心=整天关闭**，
  这个视觉语言以后要延续
- **修过一个真 bug**：平时显示的关闭框曾经比实际关闭时间短一截——因为平时按数据库里每条 `is_closed`
  记录各自的 start/end 单独画，而编辑模式用的是"合并所有半小时格生效状态"的算法，关闭记录如果存成
  几条相邻短记录，两套算法结果就对不上。已统一成都用 `calCloseWeekEffectiveRuns` 算

### 首页预约：真实空档的"简化版周表"（2026-09-04，当时新增 SQL（现网已跑）：`supabase/tea_busy_ranges.sql`）

"已是知音"卡片里不再平铺表单，只留一个"雅室预约"按钮，点开是站内弹窗（`#bookingModal`，复用
`.brand-modal` 那套）。弹窗里**照抄 admin.html 周视图的表格骨架**：左边时间轴、右边一周七天分栏、
半小时一格。跟后台的区别只有"内容阉割"：

**代码位置**：`index.html` 的 `#bookingModal` + `script.js` 里 `bwk*` 开头的一组函数 +
`styles.css` 里 `.bwk-*` 一组样式。

**视觉/结构（骨架和数值照抄后台，改之前先去 admin.html 对一眼，别自己配；下面标了
"客人版特有"的三处是 2026-09-05 按主理人要求跟后台故意做的区分）**：

| 项 | 值（= 后台 admin.html 的值，除非注明） |
|---|---|
| DOM 结构 | `.bwk-days > .bwk-day-col ×7`，跟后台 `.cal-week-wrap` 一样是 **flex 分列**。**客人版特有：没有左边那根时间轴列**（`.bwk-timecol` 已删）——客人不写备忘录、不用对着轴读行程，时间直接印在格子里，省下的 46px 分给七天。⚠️ 仍然不能用 CSS Grid 平铺格子——grid 子项的包含块是自己那一格，表头 `position:sticky` 会失效，纵向一滚"星期几"就跑没了 |
| 半小时格高 | **24px**（后台 `calWeekPxPerHour = 48` 的一半）。JS 里 `BWK_SLOT_H`、表头 `BWK_HEAD_H = 40`，**必须跟 CSS 里的 height 保持一致**，拖拽算坐标靠它们 |
| 列宽 | **58px**（`.bwk-day-col` flex-basis/min-width）。原来 46px，为了装下"晚上10:30"整串时间提到 58；因为同时砍了 46px 的时间轴列，一屏能看到的天数没变少 |
| 时间 | **客人版特有：印在每个格子里**（`.bwk-cell-time`），不是左边一根轴。格式仍是后台那套"上午"+12 小时制（`bwkFormatSlotParts`），整点 10px、半点 9px + opacity .75；**颜色是淡灰 `rgba(74,92,90,.55)`，不能用正文那种深墨色**——它只是注解，真正要一眼看出的是哪几格能点 |
| 颜色 | 边框 `#D8D0C0`、隔天底色 `#F7F3EC` / `#F8EED7`；后台时间轴那个 `#4A5C5A` 在这边只作为格子内时间的基色，永远带透明度 |
| 格子状态 | 可约 = **不刷颜色**（就是那天的底色，跟后台空格子一样）+ 印时间，不可约 = 灰色斜纹且**不印时间**，选中 = 红色框。⚠️ 别给可约格子刷底色——刷过一次，叠在隔天交替底色上变成粉红/肉色两种，整张表花掉 |
| 不可约说明标 | **客人版特有**：`.bwk-blocked-tag`，把连着的不可约格子合并成一段（`bwkBlockedRuns`），整段只写一次字——已经过去的写"已过"、被占用/主理人关了的写"已约满"（客人看不出是哪种，隐私）。文字是 `position:sticky; top:40px`，段落很长时往下滚也一直跟在表头下面。⚠️ `.bwk-blocked-tag` 上**绝对不能加 `overflow:hidden`**——sticky 只要有一个 overflow 非 visible 的祖先就改用那个当滚动容器，而它自己不滚，sticky 直接失效（踩过） |
| 一周七天 | 全部画出来，已经过去的日子**照画但整列不可点**、表头淡化；最早只能停在本周 |
| 表头 | "星期五" 完整写法 + 日期，今天用 `--seal` 标红；工具栏有 ‹ / 今天 / › |

**交互**：
- **拖拽选时间段**：鼠标按下即拖；手机长按 260ms 才进入拖拽（`BWK_LONG_PRESS_MS`，期间手指移动
  就放弃、让页面正常滚动，跟后台 `calWeekDragStart` 同一个思路）；轻点一下 = 选半小时；
  拖到被占用的格子会自动停住，不能跨过去
- **预选框**（`.bwk-selbox`）：开始时间贴框顶、结束时间贴框底、中间写时长（`bwkFormatDuration`，
  半小时说成"30分钟"），跟后台 `.cal-week-selectbox` 一样；拖的时候虚线，松手后实线。
  高度 ≥48px（选了 1 小时以上）才装得下三个标；只选半小时时框只有 24px，**只写开始时间**——
  硬塞两个标会被裁掉半行，完整的"起-止 · 共多久"由表格下面那行"已选：…"兜底
- **选中范围内的格子不重复印时间**：`.bwk-cell.masked` 把时间 `visibility:hidden`，那一段的时间
  由选框上的三个标负责，逐格再印一遍会跟它们叠在一起糊掉。这个 class 在 `bwkUpdateSelBox` 里
  按需增删（只动那几格，不重画整张表）
- **翻周不闪、零等待**：忙闲数据按"周一"为 key 缓存在 `bwkWeekCache`，每次画完还会预取
  上一周/下一周，所以点 ‹ › 基本是即时的。没缓存时也**不清空表格**，先画骨架（整列淡一点、
  暂不可点）再原地替换数据；异步结果回来先确认用户没翻走。拖拽时只更新选框那一个元素，
  不重画整张表。⚠️ 这几条是 `不许再犯.md` 第 1 条，改这块务必守住

**数据来源**：Postgres 函数 `get_tea_busy_ranges(p_start, p_end)`（`security definer`，只返回
`start_at`/`end_at` 两个字段，`grant execute` 给 `anon, authenticated`）。
**`schedule_events` 的 RLS 只给管理员读，绝不能直接对客人开放整张表**——会连会员姓名、行程标题
一起漏出去，这个函数是唯一的安全出口。

**营业时间是 9:30-23:30**（`BOOKING_BUSINESS_START_SLOT = 19`）。注意后台日历默认显示窗口是
8:30，那只是后台看行程时习惯往前多留半小时，**不是真实营业时间**，别照抄。

提交仍然进 `booking_requests` 走人工确认（`preferred_time` 存成"9月5日 周六 · 下午1:00-下午3:30"
这种文本），没有"提交=锁定时段"的强占用，多人同时提交同一空档仍可能撞车，靠掌柜确认时兜底。

**这一轮返工了五六次才做对**（下拉 → 合并大区间 chip → 半小时 chip 列表 → 表格骨架 → 参数逐项
对齐 → 拖拽和流畅度），教训已经收进 `不许再犯.md` 第 1、3 条。

## 待做事项

- [ ] 前台网页整体布局和设计（持续渐进调整）
- [ ] **可预约日历系统**（核心年/月/周日历已完成，见上，下面是还没做的部分，讨论结论已经很细，
      直接照做不用重新讨论）：
      1. 预约页面共用日历视觉但不共用内容：**已完成**（2026-09-04，见上面"首页预约：真实空档的
         简化版周表"）。剩下没做的只有"提交=锁定时段"的强占用，现在仍是人工确认兜底
      2. 主理人可以手动关闭某个时段：**已完成**（2026-09-03 返工版，见上）
      3. 茶室/琴课两个独立资源：**已完成 + 已拍板**。`schedule_events.resource` 字段和周视图左右分栏
         2026-09-02 做完；2026-09-03 主理人确认**茶室以后只做自主泡茶**，不再保留"特约主理人亲自泡茶"，
         所以琴/茶两条线永远独立、不需要额外冲突判断，这一点整个不用再动了（首页预约固定按 `tea` 这条线算）
      4. 客人预约生成的行程和主理人自己手动加的备忘录是同一张 `schedule_events` 表，只是发起方不同
      5. 可用性实时算：**已落地**——首页预约每次打开/翻周都实时查一次 `get_tea_busy_ranges`，
         后台删掉一条行程，客人那边下次打开就自动重新开放，没有额外的"重新开放"步骤
      6. 农历+传统节日节气：**已完成**（见上面 2026-09-02 的记录），月/周视图都已显示
      7. 实时天气：免费接口一般能查 3~15 天（国内城市建议和风天气/心知天气），"今天"永远排第一天，
         过去日期不显示；API 密钥不能明文写在前端代码里，需要单独设计怎么处理
      8. 固定周期排课（课程包按周固定时间上课）：主理人提过但还不确定，本质是批量生成
         `schedule_events`，不影响现有架构，想清楚了再加
- [ ] 会员分级：以后会加"线上购物会员"（不来店，直接网上买茶买茶具，主理人邮寄），跟现在的"线下学员/客人会员"权限不同。
      `profiles.member_type` 字段已经预留了口子，加新类型是加字段允许值，不影响已有数据，不用推倒重来
- [ ] 后台与前台无缝衔接：管理员身份下能直接在主页和后台之间切换（这次已经加了"返回主页"按钮，双向跳转基本打通了，
      后面主要是看还有没有细节要补）

主理人已有管理员账号，可自己管理茶器等分类的商品（含删除），不需要保留测试商品。

## 备份与回滚

每个阶段性节点都打了 git tag，出问题可以直接回滚：

```bash
git tag                          # 查看所有备份点
git checkout checkpoint-2026-08-29   # 回滚到这次的备份点（会进入 detached HEAD，确认要用再合并/新建分支）
```

已有备份点：`checkpoint-2026-08-28`、`checkpoint-2026-08-29`、`checkpoint-2026-08-30`、`checkpoint-2026-08-31`、
`checkpoint-2026-08-31-b`、`checkpoint-2026-09-01`、`checkpoint-2026-09-02`、`checkpoint-2026-09-03`、
`checkpoint-2026-09-04`（关闭时段返工）、
`checkpoint-2026-09-04-b`（关闭时段视觉定稿 + 首页预约周表：拖拽选时段、真实空档、翻周不闪）
`checkpoint-2026-08-31-b`、`checkpoint-2026-09-01`、`checkpoint-2026-09-02`
